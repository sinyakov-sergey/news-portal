package news.portal.repository;

import news.portal.entity.ArticleEntity;
import org.junit.jupiter.api.Test;

import java.util.ArrayList;

import static org.junit.jupiter.api.Assertions.*;

class ArticleRepositoryTest {
    /*
    @Test
    void getAllArticles() {
        ArticleRepository articleRepository = new ArticleRepository();
        ArrayList<ArticleEntity> list = (ArrayList<ArticleEntity>) articleRepository.getAllArticles();
        assertEquals(list.get(0).getId(), 1);
        assertEquals(list.get(0).getCategory(), "Sport");
        assertEquals(list.get(0).getHead(), "head");
        assertEquals(list.get(0).getText(), "text");
    }

    @Test
    void putInDataBase() {
        ArticleRepository articleRepository = new ArticleRepository();
        assertTrue(articleRepository.putInDataBase("sport", "head2", "text2"));
    }*/
}